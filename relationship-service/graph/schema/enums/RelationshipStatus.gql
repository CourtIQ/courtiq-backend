# Status of a relationship between users
enum RelationshipStatus {
  # No relationship exists
  NONE
  # A relationship request has been sent but not accepted
  PENDING
  # A relationship request has been accepted
  ACCEPTED
  # A relationship request has been declined
  DECLINED
  # One user has blocked the other
  BLOCKED
}